<Window x:Class="CrudClientes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CrudClientes"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button x:Name="btnCrear"
                    Content="Crear Cliente"
                    Width="120"
                    Height="30"
                    Margin="0 0 10 0"
                    Click="btnCrear_Click"/>

            <Button x:Name="btnActualizar"
                    Content="Actualizar Cliente"
                    Width="140"
                    Height="30"
                    Margin="0 0 10 0"
                    IsEnabled="False"
                    Click="btnActualizar_Click"/>

            <Button x:Name="btnEliminar"
                    Content="Eliminar Cliente"
                    Width="120"
                    Height="30"
                    IsEnabled="False"
                    Click="btnEliminar_Click"/>
        </StackPanel>

        <!-- Búsqueda -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 0 0 10">
            <TextBox x:Name="txtBuscar"
                     Width="300"
                     Height="25"
                     Margin="0 0 10 0"
                     Text="Buscar por palabra..." />

            <Button Content="Buscar"
                    Width="100"
                    Height="25"
                    Click="btnBuscar_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgClientes"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="dgClientes_SelectionChanged"
                  ItemsSource="{Binding}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Cliente" Binding="{Binding IdCliente}" Width="100"/>
                <DataGridTextColumn Header="Compañía" Binding="{Binding NombreCompañia}" Width="200"/>
                <DataGridTextColumn Header="Contacto" Binding="{Binding NombreContacto}" Width="150"/>
                <DataGridTextColumn Header="Cargo" Binding="{Binding CargoContacto}" Width="150"/>
                <DataGridTextColumn Header="Dirección" Binding="{Binding Direccion}" Width="200"/>
                <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" Width="120"/>
                <DataGridTextColumn Header="Región" Binding="{Binding Region}" Width="120"/>
                <DataGridTextColumn Header="Código Postal" Binding="{Binding CodPostal}" Width="120"/>
                <DataGridTextColumn Header="País" Binding="{Binding Pais}" Width="120"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="150"/>
                <DataGridTextColumn Header="Fax" Binding="{Binding Fax}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
